Author: Pedro Avalos Jimenez <pedro.avalosjimenez@canonical.com>
Description: add install target

--- a/Makefile
+++ b/Makefile
@@ -41,6 +41,19 @@ FILTER_OUT :=
 
 PROJECTS := $(filter-out $(FILTER_OUT),$(PROJECTS))
 
+ifeq ($(PREFIX),)
+    PREFIX := /usr/local
+endif
+
+.PHONY: all
+all: build
+	@echo "Finished building CUDA samples"
+
+.PHONY: install
+install:
+	mkdir -p $(DESTDIR)$(PREFIX)/cuda/samples/bin
+	cp -ar bin/$(TARGET_ARCH)/linux/release/* $(DESTDIR)$(PREFIX)/cuda/samples/bin/
+
 %.ph_build :
 	+@$(MAKE) -C $(dir $*) $(MAKECMDGOALS)
 
@@ -53,9 +66,6 @@ PROJECTS := $(filter-out $(FILTER_OUT),$(PROJECTS))
 %.ph_clobber :
 	+@$(MAKE) -C $(dir $*) clobber $(USE_DEVICE)
 
-all:  $(addsuffix .ph_build,$(PROJECTS))
-	@echo "Finished building CUDA samples"
-
 build: $(addsuffix .ph_build,$(PROJECTS))
 
 test : $(addsuffix .ph_test,$(PROJECTS))
